# SersmickWeatherApp

Open this app over `http://localhost`, not by double-clicking `index.html`.

Run either of these from this folder:

```powershell
node server.js
```

or double-click:

```text
start-local-server.cmd
```

Then open:

```text
http://localhost:4173
```
